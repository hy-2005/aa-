# Claude 项目协作规范

在本项目中分析、生成、修改、审查和提交代码时，必须遵守以下规则。

## 1. 提交与变更描述

- Git 提交信息必须使用中文，准确说明本次变更的目的和影响。
- Pull Request、Merge Request、变更摘要、补充说明及交付说明均应使用中文。
- 描述应优先说明“为什么修改”和“修改后有什么影响”，避免只罗列文件名或机械复述代码差异。
- 代码标识符、命令、文件路径、协议名称及无法准确翻译的专业术语可以保留原文，但其上下文说明仍须使用中文。

## 2. 代码注释规范

- 新增或修改的代码注释统一使用中文。
- 注释应重点解释设计意图、业务规则、边界条件、异常处理、兼容性要求和不易理解的实现原因。
- 不得用注释逐行复述代码，也不得添加无实际信息的注释。
- 代码与注释应保持行业实践中的合理比例：通常建议注释行数约占有效代码行数的 **15%–25%**。该比例用于指导可读性，不作为机械指标；简单、自解释的代码可以少写注释，复杂或高风险逻辑应适当增加注释。
- 应优先通过清晰的命名和合理的结构让代码自解释，注释用于补充代码本身无法充分表达的信息。
- 语言规范、工具链或第三方协议强制要求使用固定格式的注释时，可以保留其规定格式，并在必要时补充中文说明。
- 自动生成代码和第三方原始代码原则上不直接修改；如必须修改，应在允许范围内遵循本规范。

## 3. 注释与代码同步

- 代码行为、参数、返回值、边界条件或业务规则发生变化时，必须在同一次变更中检查并更新相关注释。
- 当代码与注释冲突时，以当前正确的业务需求和代码行为为依据，立即修正过时或错误的注释。
- 禁止保留已失效、具有误导性或与实现不一致的注释。
- 删除或重构代码时，应同步删除、迁移或重写相关注释。
- 代码审查时，应将注释准确性视为完成标准的一部分；注释未同步更新的变更不得视为完成。

## 4. 提交前检查

提交代码前，请确认：

- 提交信息和变更说明已使用中文。
- 新增及修改的代码注释已使用中文。
- 注释数量合理，没有为了满足比例而堆砌无意义内容。
- 所有注释均与当前代码行为一致。
- 已删除或更新过时、错误和误导性注释。
